== Scenario A: clipboard.ts MIME Discovery Logic ==
File: packages/opencode/src/cli/cmd/tui/util/clipboard.ts
Lines: 58-93

== CHECKS ==

[PASS] mimePriority array defined (line 59):
  const mimePriority = ["image/png", "image/jpeg", "image/webp", "image/gif", "image/bmp"]

[PASS] Wayland path (lines 60-75):
  - Guard: process.env["WAYLAND_DISPLAY"] && Bun.which("wl-paste")
  - Discovery: `wl-paste --list-types` → splits by \n → trims → filters empty
  - Selection: iterates mimePriority order, checks `available.includes(mime)`
  - Read: `wl-paste -t ${mime}` → .arrayBuffer() → Buffer.from(data).toString("base64")
  - Validation: checks `data && data.byteLength > 0` before returning
  - Return: { data: base64, mime }

[PASS] X11 path (lines 76-91):
  - Guard: Bun.which("xclip") (fallback from Wayland)
  - Discovery: `xclip -selection clipboard -t TARGETS -o` → splits by \n → trims → filters
  - Selection: iterates mimePriority order, checks `available.includes(mime)`
  - Read: `xclip -selection clipboard -t ${mime} -o` → .arrayBuffer() → Buffer.from(data).toString("base64")
  - Validation: checks `data && data.byteLength > 0` before returning
  - Return: { data: base64, mime }

[PASS] Fallback to text (lines 95-98):
  - `clipboardy.read()` called with .catch(() => {})
  - Returns { data: text, mime: "text/plain" } if text exists
  - Returns undefined if no clipboard content found

[PASS] Error handling:
  - All shell commands use .nothrow().quiet() — no crash on missing tools
  - clipboardy.read() has .catch fallback

== VERDICT: PASS (5/5 checks) ==
