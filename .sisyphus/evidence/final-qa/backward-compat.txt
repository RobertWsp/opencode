== Scenario D: Backward Compatibility Check ==
File: packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx

== CHECKS ==

[PASS] Single-file path handling UNCHANGED (lines 1019-1048):
  Line 1019: const isUrl = /^(https?):\/\//.test(filepath)
  Line 1020: if (!isUrl) {
  Line 1022:   const mime = Filesystem.mimeType(filepath)
  Line 1023:   const filename = path.basename(filepath)
  Line 1025:   if (mime === "image/svg+xml") { ... pasteText(content, `[SVG: ${filename}]`) }
  Line 1033:   if (mime.startsWith("image/")) { ... pasteImage({filename, mime, content}) }
  → This entire block is original code, UNCHANGED by the PR

[PASS] disable_paste_summary experimental flag preserved (line 1053):
  !sync.data.config.experimental?.disable_paste_summary
  → Exact same check as original

[PASS] [Pasted ~N lines] logic preserved (lines 1050-1058):
  const lineCount = (pastedContent.match(/\n/g)?.length ?? 0) + 1
  if ((lineCount >= 3 || pastedContent.length > 150) && ...)
    pasteText(pastedContent, `[Pasted ~${lineCount} lines]`)
  → Exact same logic as original

[PASS] Layout update workaround preserved (lines 1060-1066):
  setTimeout(() => { input.getLayoutNode().markDirty(); renderer.requestRender() }, 0)
  → Original workaround still in place

[PASS] New multi-file block (989-1018) adds behavior WITHOUT modifying existing:
  - Inserted BEFORE single-file check
  - Falls through cleanly when not applicable (no event.preventDefault if !allExist)
  - All original code paths remain reachable

[PASS] Import additions are additive only (line 5):
  import { pathToFileURL } from "bun"  ← NEW, no conflict with existing imports
  → No existing imports removed or modified

== VERDICT: PASS (6/6 checks) ==
