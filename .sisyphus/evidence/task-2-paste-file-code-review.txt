CODE REVIEW: pasteFile() Implementation
========================================

FILE: packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx

✅ VERIFICATION CHECKLIST:

1. ✅ pasteFile() function exists after pasteImage() (lines 740-793)
   - Located immediately after pasteImage() which ends at line 738
   - Correct placement confirmed

2. ✅ Uses pathToFileURL() for the URL (not data:)
   - Line 772: `const url = pathToFileURL(fp).href`
   - Generates file:///absolute/path URLs (NOT base64 data: URLs)
   - Correct implementation confirmed

3. ✅ Extmark creation matches pasteImage() pattern exactly
   - Lines 763-769: Same structure as pasteImage()
   - Uses same styleId: pasteStyleId
   - Uses same typeId: promptPartTypeId
   - Uses same produce() pattern for setStore
   - Identical pattern confirmed

4. ✅ Tilde expansion present
   - Line 744: `if (fp.startsWith("~/")) fp = path.join(process.env["HOME"] ?? "~", fp.slice(2))`
   - Correctly expands ~ to HOME directory

5. ✅ file:// URI stripping present
   - Line 743: `if (fp.startsWith("file://")) fp = decodeURIComponent(new URL(fp).pathname)`
   - Correctly strips file:// protocol and decodes URI

6. ✅ Filesystem.exists() guard present
   - Line 748: `if (!(await Filesystem.exists(fp))) return`
   - Returns silently if file not found (correct behavior)

7. ✅ pathToFileURL import added at top of file
   - Line 5: `import { pathToFileURL } from "bun"`
   - Correctly placed after path import
   - Not previously imported in this file

8. ✅ Additional features implemented:
   - Directory detection: Line 751 `const isDir = await Filesystem.isDir(fp)`
   - Virtual text for files: `[File: filename.ext]` (line 754)
   - Virtual text for directories: `[Dir: dirname/]` (line 754)
   - Relative path calculation: Line 773 `const relPath = path.relative(...)`
   - MIME type detection: Line 753 `const mime = isDir ? "inode/directory" : Filesystem.mimeType(fp)`

IMPLEMENTATION DETAILS:

Function signature:
  async function pasteFile(filepath: string): Promise<void>

Path normalization flow:
  1. Strip file:// protocol if present
  2. Expand ~ to HOME directory
  3. Resolve relative paths against sync.data.path.directory

Extmark creation:
  - Uses input.visualCursor.offset for positioning
  - Creates virtual text with [File: name] or [Dir: name/] format
  - Registers extmark with pasteStyleId and promptPartTypeId
  - Stores mapping in extmarkToPartIndex

FilePart creation:
  - type: "file" (const)
  - mime: "inode/directory" for dirs, Filesystem.mimeType() for files
  - filename: basename of path
  - url: file:///absolute/path (from pathToFileURL)
  - source.path: relative path from working directory
  - source.text: extmark position and virtual text

TYPECHECK STATUS:
  - No LSP errors detected on modified file
  - All imports resolved correctly
  - Type annotations match FilePart interface
  - Async/await syntax valid

CONCLUSION:
✅ Implementation complete and correct
✅ Follows pasteImage() pattern exactly
✅ All required features implemented
✅ Ready for integration with paste/drag handlers
